# DevServer
The development server setup file that you are using these days.


If you need to initialize compse
```console
docker-compose stop
docker-compose rm
docker system prune
docker-compose up
```
